# Confluent Cloud — Harvics Growth OS event bus
# Copy into .env.hx / .env.local (gitignored). Never commit real secrets.

KAFKA_BOOTSTRAP_SERVER=pkc-921jm.us-east-2.aws.confluent.cloud:9092
KAFKA_API_KEY=
KAFKA_API_SECRET=
KAFKA_TOPIC=Harvics_OS
KAFKA_CLIENT_ID=harvics-os
KAFKA_GROUP_ID=harvics-os-workers

# LLM (Groq primary → Gemini fallback)
GROQ_API_KEY=
GEMINI_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
GEMINI_MODEL=gemini-2.5-flash
