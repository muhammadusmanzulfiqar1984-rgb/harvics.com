'use client'

/** Pass-through — homepage scroll effects are required on the corridor home. */
export default function HomeMotionGuard({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
